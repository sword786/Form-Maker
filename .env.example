# Gemini API key used by the frontend (Vite exposes variables prefixed with VITE_).
VITE_GEMINI_API_KEY="MY_GEMINI_API_KEY"

# Optional public app URL for generating absolute links when needed.
VITE_APP_URL="https://your-app.vercel.app"
